
{% extends 'pages/_base.jinja' %}


{% block content %}

    <section id="tasks">

        {% for task in tasks %}

            <div class="task">
                    <p>{{ task.topic_name}}</p>
                    <p>{{ task.task_name }}</p>
                    <p>{{ task.task_urgency }}</p>
                    <a class="+_button" href="/task/{{ task.task_id }}/details">+</a>
            </div>

        {% else %}

            <p>No Tasks Left!</p>

        {% endfor %}

    </section>

{% endblock %}